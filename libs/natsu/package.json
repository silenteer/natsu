{
  "name": "@silenteer/natsu",
  "version": "0.0.1",
  "main": "index.js",
  "types": "index.d.ts",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "files": ["dist/**"],
  "scripts": {
    "build": "tsc && cp package.json ./dist/package.json",
    "release": "np --branch=main --no-cleanup --no-tests --no-release-draft --preview"
  },
  "dependencies": {
    "@silenteer/natsu-type": "0.0.1",
    "nats": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "16.9.1",
    "np": "7.5.0",
    "ts-node": "10.2.1",
    "tslib": "2.3.1",
    "typescript": "4.4.3"
  }
}
